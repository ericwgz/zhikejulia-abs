# 上传数据与多情景压力测试

入口 `/#/stress`。复用现有团队登录，数据集、参数快照与报告按团队隔离。
原有六只合成产品及固定报告保持独立，上传结果不覆盖原产品数据。

## 使用流程

1. 进入演示团队或自己的团队，下载空白 XLSX 模板或 3 只合成 ABS × 180 天样本。
2. 上传 CSV/XLSX，选中数据集和产品。CSV支持UTF-8/BOM、GB18030；XLSX读取
   `资产池日度` 或 `pool_daily` 工作表，否则读取第一张表。仅接受数值，不执行公式。
3. 按需要上传 PDF/DOCX/TXT 合约节选。千问只提取能匹配监控口径的明确阈值；
   提取结果须人工核对后应用，未提取项仍显示文档参考线。复杂条款不自动生效。
   DOCX/TXT由模型选择条款编号，程序从上传文本附上对应原文。
   PDF支持文字版及扫描件，单文件最多5 MB；通过千问3.8-Max原生PDF读取，异步显示进度。
   PDF结果附物理页序号和AI识别片段，可能存在OCR或页码误差，不能视为程序验证过的逐字原文；
   点击“打开原PDF”对照核验。文件内没有明确阈值时不会用历史观测值或常识补齐。
   加密、损坏、无法清晰读取或未通过结果校验的文件不会应用阈值，可重新上传相关条款页。
4. 核对3/6/12个月窗口、四类冲击参数、回收时滞与演示合同事件参数，确认后运行。
5. 服务端计算24项指标和五组情景，冻结输入哈希和方法版本；千问异步生成报告。
   上游失败或JSON/证据校验失败会保留计算结果，允许重试，不以模板冒充AI报告。
   后台报告的模型等待总预算为140秒，单次调用最多120秒，普通聊天仍为50秒。
   qwen3.8系列报告采用严格JSON Schema输出，其他Qwen模型沿用JSON Object模式。
   可用 `ABS_STRESS_LLM_MODEL` 单独设置压力模块的报告与合约提取模型（当前部署为qwen3.8-max），普通聊天沿用原模型；报告显示实际生成模型。
6. 可导出完整JSON、固定结构HTML报告（浏览器可打印为PDF）及逐期现金流CSV。

PDF后台调用最多等待300秒，页面轮询当前团队的任务；停止等待不会撤回已经发送的模型调用。
PDF文件只用于本次模型读取，应用不持久化保存原文件；未应用的解析结果在进程内暂存最多10分钟，
服务重启或过期后需重新上传。原PDF核对链接仅指向本次浏览器选中文件，离开页面后释放。
应用并运行后，阈值、识别片段及页码来源进入该次冻结报告，仍按团队隔离。
数据表仍只接受CSV/XLSX、700 KB上限，PDF不会替代24项指标所需的结构化输入。
接口采用[阿里云原生PDF理解](https://help.aliyun.com/zh/model-studio/pdf-understanding)的
Chat Completions文件消息；服务地域须支持该能力。生产运行仍仅依赖Python标准库。
PDF提取使用严格JSON Schema约束字段及类型，再由程序校验引用数字、等级顺序和页码范围；
不合格或被截断的结果不应用，诊断日志只记录固定错误类别，不记录PDF正文。

## 输入口径

一只产品每日一行，金额为人民币元，期限为月，比例以小数或百分号输入。
完整字段说明在页面和XLSX第二个工作表。主要输入为余额存量、每日现金流、
余额加权充分统计量、证券分层数据和宏观观测，不接收逐笔姓名、证件号或手机号。
只上传台账不能凭空算出宏观统计、发行预测、未来PD或合约阈值；缺失保持待评估。

新违约本金仅在首次达到90+违约口径时计一次，不能重复使用90+存量余额。
日现金流填写当天金额，累计违约/累计入池字段则是截至该日的累计值。
逾期存量采用嵌套DPD1+/30+/90+，不相加。Top10%先按贷款余额排序，取
`ceil(贷款笔数×10%)` 的本金合计；该指标是集中度，不是统计学偏度。

以报告日作为存量观察日，流量用其所在完整自然月；部分月份、断档月份不外推。
环比基期为上月末与上月完整月。连续三期负余缺必须有足够期间证据。
缺失值、零分母、无前期参照不能变成0或绿灯。

## 24项指标与计算差异

服务端 `abs_stress_calc.SPECS` 是显示名称、计算式、比较口径和默认阈值的单一来源。
每项报告包含观察值、上期值、比较值、信号、公式、阈值来源与原文证据。

| 维度 | 六项指标 |
|---|---|
| 底层贷款 | DPD30+、CDR、CCR、CPR、平均剩余期限、平均贷款余额 |
| 资产池 | 地域集中度、前10%金额集中度、缩水率、WAL、WAC、新增/到期比 |
| 偿付 | 超额利差、OC、DSC、储备金、瀑布后净余缺、利息覆盖 |
| 宏观 | 失业率、收入增速、CPI同比、消费信心、LPR、监管政策 |

文档歧义采用以下明确、可复核口径：

- CDR：日新增违约/日初未违约本金的生存率连乘，再按365/当月天数年化。
- CPR：日早偿/（日初未违约本金−计划本金）的生存率连乘，再年化。
- CCR：累计违约本金/（初始本金+累计新增入池本金），不扣回收款。
- 平均贷款期限为WAM。文档将WAL写成相同定义，本模块用真正的计划本金加权WAL；
  缺少计划现金流时只注明WAM代理，不冒充WAL。加权久期另用现金流现值加权年数。
- 缩水率为1−月末/上月末，另提供文档的余额留存比。
- WAC下降0.5%、失业率增加0.5%按百分点；信心按下降5%；LPR按同期限上调25bp。
- 监管政策为定性事件，明确的 `无新增政策（已核查）` 才表示本期已排查无事件。
- 加权PD使用同一12月预测期，并另列有PD余额覆盖率；缺少PD不以历史CDR替代。
- 原文宏观时滞的36/24/13/612可能缺少连接符，未硬编码为确定因果时滞；
  压力测试使用显式可调的统一月数，默认3个月。

方法参考：[Fannie Mae CPR/SMM](https://www.fanniemae.com/media/16541/display)、
[Fannie Mae WAL](https://www.fanniemae.com/media/16686/display)。
具体交易的违约认定和分母仍须以交易文件和实际数据字典为准。

内部总分采用四维权重40/25/25/10，各维六项等权：绿100、黄70、红35。
有一项未能评估时不出总分；任一红线令总分上限59，只有黄线令上限79。
显示整数分决定灯色：绿≥80、黄60–79、红<60。不是统计拟合PD或正式信用评级。

## 压力模型

基准+基础恶化、早偿冲击、极端违约、宏观冲击，共五组，每组3/6/12个月。
同一输入与参数产生相同数字；大模型无权改写结果。

默认封闭池、固定利率、无新增资产购买，以余额加权剩余期限作汇总摊还近似。
证券按上传剩余整数月数到期一次还本，未到期的本金提前回款在现金账户保留。
这是演示偿付计划，不能当作已经提取并完整执行了募集说明书瀑布。

正常：费用→A利息→B利息→到期A/B本金→储备补足→次级本息→可分配剩余利息。
加速：费用→A利息→B利息→A本金→B本金；法定到期前次级利息允许递延；A/B本息清偿后，支付已到期次级本息。
违约：费用→A本息→B本息→次级。状态不可逆；触发前比较当月DPD和累计违约率。
违约是否使本金立即法律到期可单独设置。复杂支付失败条款、宽限期需另行实现。

违约回收进入未来应收队列，滞后期到达后才成为现金。未收计划本金仍留在资产内。
每月验证现金、资产本金、回收应收与证券本金四套守恒关系。

报告区分：到期欠付存量、未到期本金、预计回收应收与预测本金覆盖损失。
本金覆盖损失按剩余资产价值进行独立虚拟清算：费用→A本息→B本息→次级。
不预扣次级欠息而侵蚀优先本金；次级正常还清不叫击穿。覆盖测算不含未来未实现利差。
击穿时点表示当期剩余本金首次全额失去覆盖，不等于该档初始发行本金全部损失；损失比例始终另以初始本金为分母。
早偿首先增加本金现金，不预设利息收益暴涨或优先级付款后置。

情景稳定性灯色：触发违约、出现到期欠付或A/B覆盖损失为红；只有次级受损或
加速事件为黄；否则为绿。它与历史24项指标总分分别展示。

## 报告与安全

数值结论和触发状态先由程序计算，再交给千问分析。`abs_stress_report.build_context`
向模型提供本产品24项指标的本期值、上期值、差值、判灯比较值、公式、阈值及安全余量，
同时明确百分比、百分点、倍数等单位和当前水平、环比变化等比较口径。安全余量为正表示
在安全侧，为负表示已越线；等于零是否触发仍由该指标的比较符决定，不能跨指标直接比较余量。
因此，同为绿灯的产品仍可根据离预警线的距离、变化幅度和实际现金缓冲得到不同分析。

模型还接收各情景相对基准的实际差额、到期欠付、预计本金覆盖损失、受影响档位、储备支用、
首次事件及关键月份现金流，并获得当前产品、观察期间、输入哈希、具体缺失字段和确认的情景假设。
这些内容来自本次冻结的汇总计算，不要求模型读取原始表格或自行重算24项指标；
原始政策自由文本不传给报告模型。合约PDF解析只负责提供待人工确认的阈值，不替代报告的数据上下文。

`abs_stress_narrative.SYSTEM`要求千问先选择本次有信息量的观察，再解释业务含义、可能机制与
核查方法；比较本期和前期、阈值余量、基准和压力结果，具体说明缺失数据限制了哪些判断。
稳定表现和未触发事件也应据实说明，不能把所有产品套成恶化结论。行动建议须对应本次发现，
给出优先级、原因和业务触发条件；模型不能另造预警线，也不能把相关性当作已经证实的因果。

报告正文可以直接呈现量化依据，模型优先用绑定标记引用程序数值，例如`{{M1.value}}`。
分析段落和行动原因若直接写出带单位的阿拉伯数字或日期，只有与该段已引用证据的精确显示值一致时，
程序才会将其补为已有绑定；不作近似匹配、不用其他未引用指标补数。行动触发条件不进行这种补绑定。
服务端验证标记确实存在，且对应证据已列入该节引用，然后替换为带单位的精确显示值；
已知数值标记若漏列对应来源，程序按标记补齐该来源引用，不接受未知标记或其他数据集的证据。
金额统一换算为万元，模型不得重算、抄写或自行舍入。未知标记、未绑定的数字、绕过标记的
中文量化陈述、无效或不相关的证据、重复段落和缺少基准比较均会导致校验失败。
指标名称及经程序核对的情景数量可直接表述；行动引用监控阈值时使用包含比较方向的完整条件绑定，避免把“大于”写成“大于等于”。
这些检查约束数值来源和结构，不代表已自动证实模型解释的因果关系，业务判断仍需复核。

模型响应未通过JSON结构、证据引用或数值绑定等格式校验时，服务端在剩余等待预算内
最多自动请求两次完整报告修正，将具体校验问题反馈给模型，并再次执行同一套校验。
模型等待总预算为140秒，每次最多120秒；预算不足或修正后仍不合格时保留已计算的指标和
情景结果，报告显示失败并允许用户重试，不把校验失败的正文展示为完成报告。

固定六节：执行摘要、信用质量与变化归因、结构和现金流薄弱环节、多情景比较、
瀑布事件、局限和待复核。第七节固定为管理人行动建议，每项有优先级、原因、
触发/复核条件与证据。结构固定，重点、论述和行动由模型根据本次数据选择，不是预写报告模板。
来源引用限定为服务端M/S/F/E指标情景证据与A/D假设数据证据ID，任意HTML均转义，不执行模型指令或合约内容。

成功生成的报告保存实际模型名、生成时间、提示词版本`prompt_version`、上下文版本`context_version`、
实际上下文哈希`context_hash`和格式修正次数`format_corrections`（0、1或2），用于追溯该次写作依据。
上下文及提示词的版本分别定义在`abs_stress_report.VERSION`和
`abs_stress_narrative.VERSION`。升级不会自动重写已保存报告；查看新分析时，重新选择数据集和产品
运行，生成新的报告记录。已有计算快照不变；明确重试某份报告时，只更新该份报告的AI分析。

新数据库为 `/srv/badrams-data/abs-api/stress.sqlite3`，与其他ABS数据同在系统盘。
保存归一化的汇总数据和报告，不保留原始文件。删除数据集会同时删除其报告。
每文件700KB、解压8MB、6000行、20产品；每团队10份数据集、100份报告；
全局汇总数据和报告内容分别约300MB的软上限。报告和SQLite均不进入Git。
数据库备份必须用SQLite备份接口或停写冷备，不直接复制正在写入的数据库文件。

Nginx仅 `/api/abs/work/stress/` 放宽请求到1MB，其余ABS接口保持64KB。
常规部署沿用已有环境和持久数据，不新建VM或数据盘。
